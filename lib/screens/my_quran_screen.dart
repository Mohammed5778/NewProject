import 'package:flutter/material.dart';

class MyQuranScreen extends StatelessWidget {
  const MyQuranScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('My Quran Screen'),
      ),
    );
  }
}
